apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine3.23-perl
          resources:
            requests:
              cpu: 256m
              memory: 64Mi
            limits:
              cpu: 256m
              memory: 128Mi
