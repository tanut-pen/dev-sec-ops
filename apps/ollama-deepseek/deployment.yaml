apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-deepseek-pvc
  namespace: ollama-deepseek
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama-deepseek
  namespace: ollama-deepseek
  labels:
    app.kubernetes.io/name: ollama-deepseek
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ollama-deepseek
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ollama-deepseek
    spec:
      containers:
        - name: ollama-deepseek
          image: ollama/ollama:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              ollama serve &
              until ollama list >/dev/null 2>&1; do sleep 1; done
              ollama pull deepseek-r1:1.5b
              wait
          ports:
            - containerPort: 11434
          env:
            - name: OLLAMA_KEEP_ALIVE
              value: "-1"
          volumeMounts:
            - name: ollama-data
              mountPath: /root/.ollama
      volumes:
        - name: ollama-data
          persistentVolumeClaim:
            claimName: ollama-deepseek-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ollama-deepseek
  namespace: ollama-deepseek
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: ollama-deepseek
  ports:
    - port: 11434
      targetPort: 11434
      nodePort: 30114
