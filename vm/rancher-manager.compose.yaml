services:
  rancher:
    image: rancher/rancher:${RANCHER_VERSION:-latest}
    container_name: rancher-manager
    privileged: true
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - rancher_data:/var/lib/rancher

volumes:
  rancher_data:
